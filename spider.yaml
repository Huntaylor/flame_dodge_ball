# Generated by Spider
# For more info on configuration, visit https://birjuvachhani.github.io/spider/grouping/
# Generates unit tests to verify that the assets exists in assets directory
generate_tests: false

# Use this to remove vcs noise created by the `generated` comments in dart code
no_comments: true

# Exports all the generated file as the one library
export: true

# This allows you to import all the generated references with 1 single import!
use_part_of: true

# Generates a variable that contains a list of all asset values.
use_references_list: false

# Generates files with given ignore rules for file.
# ignored_rules:
#   - public_member_api_docs

# Generates dart font family references for fonts specified in pubspec.yaml
# fonts: true
# -------- OR --------
# fonts:
#   class_name: MyFonts
#   file_name: my_fonts

# Location where all the generated references will be stored
package: ../packages/assets/lib/assets

groups:
  # dodge balls
  - path: assets/sprites/dodge_balls
    class_name: BallSprites
    types: [.aseprite]
  - path: assets/images/dodge_balls
    class_name: BallImages
    types: [.png]

  # audio
  - path: assets/audio
    class_name: Audio
    types: [.mp3]

  # boss
  - path: assets/images/boss
    class_name: BossImages
    types: [.png]
  - path: assets/sprites/boss
    class_name: BossSprites
    types: [.aseprite]
  - path: assets/animations/boss
    class_name: BossAnimations
    types: [.json]

  # enemies
  - path: assets/images/enemies
    class_name: EnemySprites
    types: [.png]
  - path: assets/animations/enemy
    class_name: EnemyAnimations
    types: [.json]

  # platform
  - path: assets/animations/platform
    class_name: PlatformAnimations
    types: [.json]
  - path: assets/images/platform
    class_name: PlatformImages
    types: [.png]
  - path: assets/sprites/platform
    class_name: PlatformSprites
    types: [.aseprite]

  # player
  - path: assets/animations/players
    class_name: PlayerAnimations
    types: [.json]
  - path: assets/images/players
    class_name: PlayerImages
    types: [.png]
  - path: assets/sprites/players
    class_name: PlayerSprites
    types: [.aseprite]
