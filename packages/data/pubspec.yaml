name: data

publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.10.4

resolution: workspace

dependencies:
  audioplayers: ^6.5.1
  domain:
    path: ../domain
  flutter:
    sdk: flutter
  get_it: ^9.2.0
  get_it_injector: ^0.6.0

dev_dependencies:
  build_runner: ^2.10.4
  get_it_injector_gen: ^0.6.0
